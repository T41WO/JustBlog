@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, 
    IList<JustBlog.Core.Models.Tag> tags)
{
  foreach (var tag in tags)
  {
    <div class="tags-div">
        @JustBlog.CustomHelpers.ActionLinkExtensions.TagLink(htmlHelper, tag)
    </div>
  }
}